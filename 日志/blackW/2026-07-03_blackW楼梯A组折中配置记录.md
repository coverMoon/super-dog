# 2026-07-03 blackW 楼梯 A 组折中配置记录

## 1. Jun26 与 Jul01 楼梯表现折中 A 组

根据 `Jun26_12-30-56_` 与 `Jul01_03-04-42_` 的实机现象对比，当前先做 A 组小步调整。`Jun26_12-30-56_` 姿态较稳但后轮容易卡脚；`Jul01_03-04-42_` 后腿脱困链路更完整，但有前腿被压住和上楼梯前倾的倾向。因此本轮从 `Jul01_03-04-42_` 继续，保留其虚位随机化、楼梯多轮协调、`clearance_margin = 0.065`、`progress_weight = 0.55`、`rear_lift_target_offset = 0.02` 等主体设置，只调整 pair escape 的前后腿平衡。

关键配置：`load_run = Jul01_03-04-42_`，`max_iterations = 1000`；`stairs_pair_escape` scale 从 1.5 降到 1.3，`front_pair_weight` 从 0.7 提到 0.9，`rear_pair_weight` 从 2.8 降到 2.3；`stairs_run_still` 设回 `Jul01_03-04-42_` 保存配置中的 -0.8，不继续使用当前文件里更强的 -1.2。`stairs_rear_target_bonus` 与 `stairs_rear_stuck_escape` 继续关闭。

本组目标是保留 Jul01 后腿最终脱困的收益，同时降低后腿 reward 过度主导，给前腿更多 pair escape credit，观察前腿卡住和前倾是否缓解。重点看实机前腿越台阶沿是否更自然、后轮是否仍能抬上来、上楼梯 pitch 是否减轻，以及训练侧 `rew_stairs_pair_escape`、`rew_progress`、`dof_pos_limits`、`foot_impact_vel`、`orientation` 是否出现退化。若 A 组前腿明显改善但仍前倾，再进入 B 组轻调 `stairs_run_still` 和 orientation 衰减；若后腿又变得卡住，则优先小幅回升 `rear_pair_weight` 到 2.5，而不是继续加后腿 target offset。

## 2. A 组基础上的 B 组姿态释放配置

在 A 组 pair escape 平衡配置基础上切到 B 组，继续保留 `load_run = Jul01_03-04-42_`、`stairs_pair_escape = 1.3`、`front_pair_weight = 0.9`、`rear_pair_weight = 2.3`，不改 lift 高度、后腿 target offset、虚位随机化和多轮协调。本组只处理前腿被压住与前倾之间的姿态约束关系。

关键配置：`stairs_run_still` 从 -0.8 降到 -0.4，减少楼梯上非轮关节回中对前腿起抬/贴障滚越的压制；`orientation_terrain_sigma` 从 0.0010 回到 0.0015，`orientation_terrain_min_scale` 从 0.03 回到 0.05，让楼梯复杂接触下的 pitch 惩罚不要衰减得过快。目标是释放前腿动作余量，同时比 Jul01 保存配置略收紧姿态，缓解上楼梯前倾。

观察重点：如果 B 组前腿更容易越过台阶沿、pitch 前倾减轻且后腿仍能最终脱困，则说明 A 组的 pair 平衡加上轻量姿态释放更接近折中解；如果姿态变稳但前腿仍卡，下一步应考虑进一步降低 `stairs_run_still` 或单独提高前腿 pair credit；如果后腿重新卡住，则优先回到 A 组或小幅提高 `rear_pair_weight`，不建议直接加高 `rear_lift_target_offset`。

## 3. A/B 组 sim2real 反馈

实机反馈显示，A 组 `Jul03_02-18-30_` 的前倾有缓解，说明降低后腿 pair 主导、提高前腿 pair credit 的方向有效；但它必须很慢地上楼梯，并且上一会需要停零点几秒，说明连续推进和连续脱困能力不足。结合训练侧 A 组 `terrain_level` 与 `mean_reward` 仍优于 B 组，这个停顿更像是 pair 脱困强度降低后，策略需要在台阶沿重新摆正/回中再进入下一次脱困，而不是单纯姿态太松。

B 组 `Jul03_02-19-38_` 表现较差，说明继续降低 `stairs_run_still` 并收紧 orientation 衰减没有带来实机收益。后续不建议沿 B 组方向继续放松楼梯回中；应回到 A 组作为基线，围绕“保持前倾改善，同时恢复连续上楼梯速度”做小步调整。

下一轮优先建议从 A 组继续，保留 `front_pair_weight = 0.9`，把 `stairs_pair_escape` 从 1.3 小幅回升到 1.4，并把 `rear_pair_weight` 从 2.3 小幅回升到 2.5；`stairs_run_still` 保持 -0.8，orientation 衰减保持 A 组配置。目标是恢复连续脱困驱动力，同时不回到 Jul01 那种后腿过度主导和前倾状态。若仍出现停顿，可再考虑轻量提高 `stairs_multi_contact_progress`，但不应优先回到 B 组姿态释放路线。

## 4. A 组连续推进增强配置

根据 A/B 组实机反馈，当前配置回到 A 组路线并做小幅连续推进增强。从 `Jul03_02-18-30_` 继续训练 1000 iter，保留 A 组中缓解前倾有效的 `front_pair_weight = 0.9`、`stairs_run_still = -0.8`、`orientation_terrain_sigma = 0.0010`、`orientation_terrain_min_scale = 0.03`，不再沿 B 组降低楼梯回中或收紧 orientation 衰减。

本轮只把脱困驱动力略微补回：`stairs_pair_escape` 从 1.3 提到 1.4，`rear_pair_weight` 从 2.3 提到 2.5，仍低于 Jul01 的 `stairs_pair_escape = 1.5`、`rear_pair_weight = 2.8`。目标是减少 A 组上楼梯中途停顿，恢复连续台阶推进，同时尽量保留前倾缓解效果。

观察重点：实机是否能连续上楼梯、不再每几级停零点几秒；前倾是否仍低于 Jul01；后腿是否保持足够脱困而不重新压住前腿。训练侧重点看 `rew_stairs_pair_escape` 是否小幅回升，`rew_progress` 和 `terrain_level` 是否不低于 A 组，同时关注 `dof_pos_limits`、`foot_impact_vel`、`orientation` 是否因后腿权重回升而变重。

## 5. Jul03_14-58-08_ 训练结果初查

`Jul03_14-58-08_` 从 A 组 `Jul03_02-18-30_` 继续训练到 `model_43000.pt`，使用连续推进增强配置：`stairs_pair_escape = 1.4`、`front_pair_weight = 0.9`、`rear_pair_weight = 2.5`，`stairs_run_still = -0.8`，orientation 衰减保持 A 组的 `sigma = 0.0010`、`min_scale = 0.03`。

末 100 iter 对比 A 组，`terrain_level` 从约 5.443 提到 5.463，`rew_progress` 从约 0.509 提到 0.516，`rew_stairs_pair_escape` 从约 0.0335 提到 0.0448，`rew_stairs_multi_contact_progress` 也从约 0.0222 提到 0.0237。这说明小幅回升 pair escape 和后腿权重确实增强了连续推进/脱困信号，训练侧符合减少中途停顿的目标。

风险信号也更明显：`mean_reward` 从约 74.38 降到 73.17，`wheel_obstacle_spin` 从约 -0.089 变重到 -0.102，`action_rate`、`smoothness`、`dof_acc`、`collision` 也比 A 组更重；`orientation` 从 A 组约 -0.163 变重到 -0.175，但仍略好于 Jul01 父模型约 -0.182。`dof_pos_limits` 反而比 A 组轻一些，value loss 末 100 约 0.241，末段没有发散。

当前判断：`Jul03_14-58-08_` 值得进 sim2real，重点验证 A 组的“慢慢上、上一步停一下”是否改善。如果实机连续性明显变好且前倾仍低于 Jul01，可以作为下一轮基线；如果连续性改善但空转/冲击或前倾回潮明显，下一步不应继续加 `rear_pair_weight`，可考虑回到 `rear_pair_weight = 2.4` 或保持 2.5 但把 `wheel_obstacle_spin`/动作平滑轻量补回来。
