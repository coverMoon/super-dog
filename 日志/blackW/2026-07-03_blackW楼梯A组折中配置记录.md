# 2026-07-03 blackW 楼梯 A 组折中配置记录

## 1. Jun26 与 Jul01 楼梯表现折中 A 组

根据 `Jun26_12-30-56_` 与 `Jul01_03-04-42_` 的实机现象对比，当前先做 A 组小步调整。`Jun26_12-30-56_` 姿态较稳但后轮容易卡脚；`Jul01_03-04-42_` 后腿脱困链路更完整，但有前腿被压住和上楼梯前倾的倾向。因此本轮从 `Jul01_03-04-42_` 继续，保留其虚位随机化、楼梯多轮协调、`clearance_margin = 0.065`、`progress_weight = 0.55`、`rear_lift_target_offset = 0.02` 等主体设置，只调整 pair escape 的前后腿平衡。

关键配置：`load_run = Jul01_03-04-42_`，`max_iterations = 1000`；`stairs_pair_escape` scale 从 1.5 降到 1.3，`front_pair_weight` 从 0.7 提到 0.9，`rear_pair_weight` 从 2.8 降到 2.3；`stairs_run_still` 设回 `Jul01_03-04-42_` 保存配置中的 -0.8，不继续使用当前文件里更强的 -1.2。`stairs_rear_target_bonus` 与 `stairs_rear_stuck_escape` 继续关闭。

本组目标是保留 Jul01 后腿最终脱困的收益，同时降低后腿 reward 过度主导，给前腿更多 pair escape credit，观察前腿卡住和前倾是否缓解。重点看实机前腿越台阶沿是否更自然、后轮是否仍能抬上来、上楼梯 pitch 是否减轻，以及训练侧 `rew_stairs_pair_escape`、`rew_progress`、`dof_pos_limits`、`foot_impact_vel`、`orientation` 是否出现退化。若 A 组前腿明显改善但仍前倾，再进入 B 组轻调 `stairs_run_still` 和 orientation 衰减；若后腿又变得卡住，则优先小幅回升 `rear_pair_weight` 到 2.5，而不是继续加后腿 target offset。

## 2. A 组基础上的 B 组姿态释放配置

在 A 组 pair escape 平衡配置基础上切到 B 组，继续保留 `load_run = Jul01_03-04-42_`、`stairs_pair_escape = 1.3`、`front_pair_weight = 0.9`、`rear_pair_weight = 2.3`，不改 lift 高度、后腿 target offset、虚位随机化和多轮协调。本组只处理前腿被压住与前倾之间的姿态约束关系。

关键配置：`stairs_run_still` 从 -0.8 降到 -0.4，减少楼梯上非轮关节回中对前腿起抬/贴障滚越的压制；`orientation_terrain_sigma` 从 0.0010 回到 0.0015，`orientation_terrain_min_scale` 从 0.03 回到 0.05，让楼梯复杂接触下的 pitch 惩罚不要衰减得过快。目标是释放前腿动作余量，同时比 Jul01 保存配置略收紧姿态，缓解上楼梯前倾。

观察重点：如果 B 组前腿更容易越过台阶沿、pitch 前倾减轻且后腿仍能最终脱困，则说明 A 组的 pair 平衡加上轻量姿态释放更接近折中解；如果姿态变稳但前腿仍卡，下一步应考虑进一步降低 `stairs_run_still` 或单独提高前腿 pair credit；如果后腿重新卡住，则优先回到 A 组或小幅提高 `rear_pair_weight`，不建议直接加高 `rear_lift_target_offset`。
