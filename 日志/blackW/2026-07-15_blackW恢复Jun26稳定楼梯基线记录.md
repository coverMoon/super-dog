# 2026-07-15 blackW 恢复 Jun26 稳定楼梯基线记录

本地训练代码重新恢复到 `Jun26_12-30-56_` 对应的 commit `3f0214b`。除 `blackW_config.py`、`blackW_env.py` 外，本次同时恢复其直接继承依赖 `black_config.py`、`black_env.py` 和 `base/legged_robot.py`，避免继续继承 Jul07 后加入的 probe command、分轴 COM 随机化等行为。

训练参数只保留目标 run 保存时的 dirty 改动 `wheel_obstacle_unloaded_lift = -0.05`。楼梯关键参数恢复为 `horizontal_force_threshold = 15.0`、`wheel_obstacle_spin = -1.0`、`stairs_continuous_scale = 1.5`。runner 设置为 `resume = True`、`load_run = "Jun26_12-30-56_"`、`checkpoint = -1`，用于从该稳定模型继续训练；这两项与当时用于生成该 run 的父 run 入口不同，但不改变环境和 reward 配置。

风险：恢复的 `black_config.py`、`black_env.py` 和 `legged_robot.py` 是共享层，本地 `black` 任务也会回到 Jun26 对应行为。若后续切回 `black` 实验，应先恢复其当前主线版本。

验证：除上述 `blackW_config.py` 两类必要差异外，5 个相关文件与 `3f0214b` 一致，其中 4 个未补参数的文件逐字节一致；`python3 -m py_compile` 检查通过。`git diff --check` 仅报告 Jun26 原始 `black_config.py` 已存在的两处行尾空格，为保持基线一致未做格式清理。
