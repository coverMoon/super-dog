# 2026-06-22 blackW 配置回退记录

## 1. 回退到 Jun21_15-30-25_ 行为配置

按 `Jun21_15-30-25_/config.json` 恢复当时的主要训练行为参数：`run_still = -2.0`、`stairs_run_still = -3.0`、`wheel_obstacle_lift = 1.75`、`wheel_obstacle_spin = -1.5`、`action_rate = -0.09`、`entropy_coef = 0.0035`。

当前 `stairs_run_still` reward 实现与目标 run 一致，不需要修改 env 逻辑。保留当前源码中的 `num_envs`、`resume` 和 `load_run` 默认值；目标 run 记录的 8192 env 与 `load_run = Jun20_21-27-59_` 来自当次启动命令行，不固化到配置源码。

预期观察：重点关注 `rew_run_still`、`rew_stairs_run_still`、`rew_wheel_obstacle_lift`、`rew_wheel_obstacle_spin`、`rew_action_rate`、`rew_smoothness`、`rew_torques`、速度跟踪、楼梯通过率及 value loss。如训练后期再次出现 action/smoothness/torques 同步恶化，优先回退 `action_rate` 和 `wheel_obstacle_spin` 两项。
