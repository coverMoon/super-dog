# HIMLoco 视觉/地形感知增强训练方案（更新版）

## 1. 背景与目标

当前使用 HIMLoco 开源代码训练自研四足机器人。目标地形不再局限于普通 rough terrain，而是包含更高难度的障碍类型，例如：

- 连续 gap 地形
- 高墙 / 高台阶
- 窄落脚区域
- 混合障碍地形

这类任务的共同特点是：**仅靠本体感觉通常难以在接触前获得足够的前方地形信息**。  
HIMLoco 的优势在于本体感觉主导的高频稳定控制、扰动适应和较好的 sim-to-real 特性，但在需要“提前预判未来几步可通行几何”的任务中会存在信息瓶颈。

因此，本方案的核心目标是：

1. 在尽量保留 HIMLoco 主干优势的前提下，引入地形感知信息。
2. 验证局部地形几何表示是否能够显著提升连续 gap / 高墙任务表现。
3. 验证感知信息应如何接入系统：只进 actor、还是同时进入 internal model。
4. 构建一条从仿真 oracle 地形到未来真实传感器部署都可兼容的训练路线。

---

## 2. 总体思路

### 2.1 不直接做“原始视觉端到端”

本阶段不建议一开始就直接把原始图像或深度图送入策略网络，原因包括：

- 训练不稳定
- 维度高、冗余多
- PPO 的信用分配更困难
- 难以区分“视觉表示无效”还是“感知链路未调好”

因此，优先采用**统一的局部地形几何表示**，例如：

- 固定采样高度点（推荐首选）
- 小尺寸局部 height patch
- robot-centric elevation map 上的局部采样

### 2.2 训练目标不是“学习某个传感器”，而是“学习几何”

策略不应依赖“这是深度相机的图像”还是“这是激光雷达的点云”，而应依赖统一抽象后的地形表示：

`任意传感器 -> 统一局部高度表示 -> terrain branch -> policy`

这样做的好处是：

- 训练期可使用仿真 oracle 地形
- 部署期可由不同传感器生成同一种 terrain representation
- 算法与感知链路解耦

### 2.3 继承 HIMLoco，但不原样照搬 PIM

PIM 的核心启发是：**地形信息不应只进入 actor，也可以进入 internal model / state estimation**。  
但对当前四足连续 gap / 高墙任务，更推荐如下改造：

- 保留 HIM 的 proprio/history 主干
- 单独增加 terrain branch
- 不做过早融合
- 在 actor 末端、以及 internal model 的条件输入处做后融合
- 允许未来部署时 terrain 以低频异步更新

---

## 3. 核心研究问题

本方案主要围绕以下问题展开：

### Q1. 局部地形信息是否真的能显著提升任务表现？
需要验证在连续 gap / 高墙任务中，oracle terrain representation 相比 blind HIM 是否有明显增益。

### Q2. 地形信息应如何接入？
需要比较：

- 直接拼接到 actor observation
- 使用独立 terrain encoder 后融合
- 除 actor 外，同时接入 internal model

### Q3. 连续 gap 是否真正需要记忆？
需要验证连续 gap 任务中，单步地形信息是否足够，还是必须引入 terrain memory（如 GRU）。

### Q4. 后续部署时异步感知是否会成为瓶颈？
虽然当前阶段先不考虑部署，但训练阶段应提前考虑 terrain 更新频率较低、存在延迟与空洞的问题。

### Q5. 局部地形应该用什么参考系表示？
需要明确地形高度应表示为：

- 相对于全局地面绝对高度
- 相对于机器人 base 的局部高度差
- 机器人中心且重力对齐的局部 elevation 表示

### Q6. 采样区域是否应受真实传感器可见域限制？
需要明确训练中局部地形采样区域是：

- 先按理想 robot-centric 区域定义
- 还是一开始就严格贴近未来相机/雷达的真实视场与外参

---

## 4. 地形表示设计原则

## 4.1 不使用全局绝对高度作为主要策略输入

不建议将地形高度定义为“相对世界坐标系地面零点的绝对高度”。原因包括：

- 平台、台阶、坡面会导致全局高度整体漂移
- 策略真正关心的是局部地形相对当前机体的几何关系
- 部署时很难稳定定义“全局地面零点”
- 容易让策略学到与任务无关的绝对高度偏置

因此，训练和部署都不建议依赖绝对地面高度。

## 4.2 推荐使用 robot-centric + gravity-aligned + relative-to-base 的局部表示

推荐的地形表示应满足三个条件：

1. **Robot-centric**：地图中心跟随机器人移动
2. **Gravity-aligned**：地图竖直方向与重力方向对齐，而不是随机身 pitch / roll 旋转
3. **Relative-to-base**：每个采样点的值表示其相对于当前 base 高度的高差

可写为如下形式：

`h_i = z_terrain(x_i, y_i) - z_base`

其中：

- `(x_i, y_i)` 是机器人周围局部地图中的采样位置
- `z_terrain(x_i, y_i)` 是该位置在重力对齐局部地图中的高度
- `z_base` 是当前 base 在同一参考系中的高度

这种表示的优点是：

- 更符合 locomotion 所需的局部几何信息
- 输入分布更稳定
- 更适合跨平台、台阶、gap、坡面等复杂场景
- 部署时更容易由视觉/雷达地图生成

## 4.3 首版可只使用“相对高度”，后续再扩展

首版输入建议只使用：

- 相对 base 的高度值

后续可扩展加入：

- 有效性 mask
- terrain age
- 局部坡度 / 高度差特征
- 地图质量相关标量

---

## 5. 采样区域与真实传感器可见域的关系

## 5.1 当前阶段可以先按理想 robot-centric 区域采样

在训练前期，主要目标是验证方法本身是否有效，因此**不必一开始就严格按真实相机/雷达视场定义采样区域**。

第一阶段可直接：

- 在 base 坐标系下定义固定局部采样区域
- 使用仿真 oracle terrain 生成采样点
- 先验证 terrain information 是否显著提升控制效果

这样做的意义是：

- 优先验证控制结构与学习路线
- 避免过早被真实感知链路复杂性干扰
- 防止因“视场建模不完整”误判方法无效

## 5.2 但不能永远忽略真实可见域

如果后续始终假设机器人周围地形“全都可见”，则容易得到一个过于理想化的策略，即：

- 依赖全知局部地图
- 没有见过真实遮挡和空洞
- 对外参偏差、斜向安装、视场边界很敏感

因此，中后期必须逐步将以下因素纳入训练：

- 可见区域裁剪
- 遮挡导致的缺失区域
- 外参偏差
- 斜向下安装导致的覆盖区域变化
- 感知频率较低带来的地形陈旧问题

## 5.3 对“当前拿不到精确外参”的判断

当前暂时拿不到相机/雷达相对机身坐标偏移，这**不会阻止当前训练方案推进**。  
因为现阶段重点仍是方法验证，而不是完整复现真实感知。

但这会在后期带来两类问题：

1. 无法准确判断哪些区域未来真实传感器稳定可见
2. 无法准确模拟地图投影到 body frame 时的误差

因此，当前阶段可以先不依赖精确外参，但后续必须补上外参现实性建模。

## 5.4 头部斜向下安装的影响

相机安装在头部且斜向下，会带来以下影响：

- 可见域不再对称
- 前近场与前远场覆盖密度不同
- 近机身区域可能被机体部分遮挡
- 随着机体 pitch / roll 变化，局部可见区域也会变化
- 后腿附近地形往往更依赖历史地图或记忆

这说明：

- 斜向下安装不是致命问题
- 但未来训练中必须处理“不可见区域”和“信息新鲜度”
- 对连续 gap 这类任务，terrain memory 更重要

---

## 6. 网络结构设计

## 6.1 总体结构

推荐采用“双分支 + 后融合”的结构：

- **Proprio branch**：沿用 HIMLoco 的 proprio/history encoder
- **Terrain branch**：独立编码局部地形表示
- **Fusion stage**：在 actor 末端、以及 internal model 条件输入处做融合

整体逻辑如下：

1. proprio/history 输入进入 HIM 主干，得到 `z_prop`
2. terrain representation 进入 terrain encoder，得到 `z_terrain`
3. actor 使用 `concat(z_prop, z_terrain, freshness, command, phase)` 输出动作
4. internal model 在需要时以 `z_prop + z_terrain` 作为条件输入，进行 next-state 预测

---

## 6.2 Proprio branch（保留 HIM 主干）

该分支尽量少改动，负责高频稳定控制。

### 输入建议

- projected gravity
- base angular velocity
- joint position error
- joint velocity
- previous action
- command velocity
- gait phase / clock input
- history stack

### 输出

- `z_prop`：表示机体动态、接触、扰动响应等高频本体信息的隐向量

---

## 6.3 Terrain branch（新增）

该分支负责编码局部地形几何。

### 推荐首版输入形式：固定采样高度点

优先推荐使用 64～128 个固定采样点，首版建议 **96 点**。

采样原则：

- 采样区域定义在机器人 base 坐标系下
- 前方区域采样更密
- 预计前后足落脚带更密
- 机器人正下方与障碍边缘附近更密
- 后方可适当稀疏

每个采样点初始只提供：

- 相对 base 的高度值 `h_i`

附加辅助输入：

- `mask_i`：该点是否有效
- `terrain_age`：当前地形信息距最近一次更新已过去多久

### 备选输入形式：小尺寸局部 height patch

如 15×15 或 21×21 的局部高度图，可在后续实验中使用轻量 CNN 编码。  
但首版更建议从固定采样点开始，因为：

- 输入维度更低
- 更方便调试
- 更适合做结构对比实验
- 更方便未来替换为真实地图采样

---

## 6.4 Terrain encoder

建议首版采用小型 MLP：

- 输入：96 维高度点 + mask + age
- 输出：`z_terrain`

之后可扩展为：

- MLP + GRU
- CNN + MLP
- CNN + GRU

---

## 6.5 Terrain memory（针对连续 gap）

连续 gap 的本质难点之一在于：  
当前相机看到的几何，可能会在几步后影响后腿落脚；因此策略可能需要记住“之前看到但当前已经离开视野”的局部地形。

因此，推荐在后续版本中引入：

- `terrain encoder -> GRU -> z_terrain_mem`

使用方式：

- terrain 分支的 GRU 仅在 terrain 更新时更新
- 无更新时保持 hidden state 不变
- actor 每个 control step 读取最近的 `z_terrain_mem`

### 预期作用

- 提升连续 gap、长落脚间隔任务表现
- 缓解前视观测对后腿落脚信息不完整的问题

---

## 6.6 Fusion 方式

不建议过早融合。推荐后融合：

`z_fused = concat(z_prop, z_terrain(or z_terrain_mem), terrain_age, command, phase)`

然后：

- actor head：由 `z_fused` 输出动作
- critic：可使用更丰富的 privileged terrain 信息
- internal model：在特定版本中条件化使用 terrain latent

---

## 7. Internal Model 的改造思路

## 7.1 原始 HIM 的特点

原始 HIM 的核心是：

- 使用 proprio history
- 估计下一时刻线速度
- 学习下一步本体状态的 latent 表征
- 通过 internal model 改善策略训练

## 7.2 本方案的改造思路

不建议把 terrain 和 proprio 从最开始就混成同一个 encoder，而建议做条件化改造：

### 改造版 internal model 输入

- `z_prop`
- `z_terrain` 或 `z_terrain_mem`

### internal model 可预测的量

- next base linear velocity
- next proprio latent / next proprio state embedding
- 可选：future contact pattern
- 可选：terrain difficulty indicator
- 可选：future foot clearance demand

### 核心意义

让 terrain 信息不仅影响 actor，还影响系统对“下一步将会发生什么”的估计。  
这比只把 terrain 作为 actor 的附加观测更符合 PIM 的核心思想，但比“完全早融合”更稳定。

---

## 8. 训练阶段设计

整个训练建议分为五个阶段，逐步验证每个关键假设。

## Stage 0：Blind baseline

### 目标

建立纯 HIMLoco 基线。

### 任务

- 单个小 gap
- 单个低台阶
- 初级 rough terrain

### 记录指标

- 成功通过率
- 平均前进距离
- 跌倒率
- episode return
- 速度跟踪误差

---

## Stage 1：Oracle terrain 直接输入

### 目标

先验证“地形信息本身是否有价值”。

### 方法

从仿真环境直接读取局部地形，构造 96 点高度采样，直接拼接到 actor observation 中。

### 对比

- Blind HIM
- HIM + terrain direct concat

### 重点观察

- 单个 gap 成功率是否显著提高
- 单个高墙/台阶通过率是否显著提高
- 是否出现明显更主动的抬腿、跨越动作

### 若无明显提升，说明可能问题在于

- reward 设计不对
- curriculum 不合适
- 动作空间不足
- 任务难度过高

---

## Stage 2：独立 terrain branch

### 目标

验证“独立感知分支”是否优于直接拼接。

### 方法

增加 terrain encoder，结构为：

- proprio branch -> `z_prop`
- terrain branch -> `z_terrain`
- actor 末端后融合

### 对比

- Stage 1 的 direct concat
- Stage 2 的 independent terrain branch

### 重点观察

- 收敛速度
- 地形任务上的稳定性
- blind 任务是否被破坏
- terrain 信息是否更容易被利用

---

## Stage 3：Terrain 进入 internal model

### 目标

验证 PIM 的核心思想在四足任务里是否有效。

### 方法

在 Stage 2 基础上，让 internal model 在 next-state 预测时也使用 terrain latent。

### 对比

- Stage 2：terrain 只进 actor
- Stage 3：terrain 同时进 actor + internal model

### 重点观察

- 连续 gap 成功率
- 高墙通过率
- 训练效率
- next-state estimation 准确性（若有相关指标）

### 判断标准

如果 Stage 3 明显优于 Stage 2，说明：

- terrain 不仅是“策略参考”，也是“状态估计条件”
- PIM 的核心思想对四足任务也有价值

如果二者差异不明显，则可以保留更简单的 Stage 2 结构。

---

## Stage 4：Terrain memory（GRU）

### 目标

验证连续 gap 任务是否真的需要时序记忆。

### 方法

terrain branch 增加 GRU：

- terrain encoder -> GRU -> `z_terrain_mem`

### 对比

- 无 GRU
- 有 GRU

### 重点观察

- 连续双 gap / 多 gap 成功率
- 后腿落脚稳定性
- 是否减少“前腿通过但后腿踩空”的失败模式

### 预期

- 对单个 gap / 单台阶，收益可能有限
- 对连续 gap，收益更可能明显

---

## Stage 5：异步更新与噪声鲁棒性

### 目标

提前让策略适应未来真实感知链路的特性。

### 训练中逐步加入

- terrain 每 K 个 control step 才更新一次
- sample-and-hold
- `terrain_age` 持续增大
- 随机丢帧
- 高度噪声
- 局部地图空洞
- 有效区域 mask
- 可选：局部坐标误差
- 可选：虚拟视场裁剪
- 可选：外参偏差随机化

### 意义

虽然当前阶段不直接考虑部署，但这一步能避免策略过拟合到“每一步都有新鲜、完美 terrain”的理想条件。

---

## 9. 任务与 curriculum 设计

## 9.1 不建议一开始混合所有障碍

不同障碍所需能力不同：

- 连续 gap 更强调地形记忆与未来落脚推断
- 高墙/高台更强调前足 clearance、机体姿态调整与越障动作

因此应采用分阶段 curriculum。

## 9.2 Curriculum 建议

### Level 1：基础任务

- 单个小 gap
- 单个低台阶
- 小平台

### Level 2：随机参数任务

- gap 宽度变化
- 台阶高度变化
- 窄落脚区域

### Level 3：组合任务

- 连续双 gap
- gap 间距变化
- 连续台阶 / 高墙

### Level 4：复杂混合任务

- 连续 gap + 高墙混合
- 随机组合障碍
- 加入部分地图缺失 / terrain 噪声

---

## 10. Reward 设计建议

## 10.1 原则

不能只依赖普通 locomotion reward，否则策略可能：

- 忽略 terrain 信息
- 继续依靠保守试探式 gait
- 学不到真正的“提前预判”

因此需要加入任务相关 shaping。

## 10.2 Gap 任务相关 reward

- 足端落入 gap：强惩罚
- 落脚靠近危险边缘：惩罚
- 成功跨过目标 gap：事件奖励
- 合理 foot clearance：轻量奖励

## 10.3 高墙 / 高台任务相关 reward

- 前足成功越上障碍：奖励
- 机体撞墙：惩罚
- 足端无效拍墙：惩罚
- 成功登顶：事件奖励

## 10.4 通用 reward

- 前进速度跟踪
- 姿态稳定
- 动作平滑
- 能耗约束

### 注意

动作正则不要过强，否则策略会不愿执行大步跨越、较大抬腿或明显前倾等必要动作。

---

## 11. 观测设计建议

## 11.1 Actor 输入

### Proprio 部分

- projected gravity
- base angular velocity
- command velocity
- joint position error
- joint velocity
- previous action
- gait phase / clock input
- history stack

### Terrain 部分

首版建议：

- 96 个高度采样点（相对 base 的高度）
- valid mask
- terrain age

### Fusion 后输入到 actor head

- `z_prop`
- `z_terrain` 或 `z_terrain_mem`
- terrain_age
- command
- phase

---

## 11.2 Critic 输入

critic 可适当更强一些，训练时可使用 privileged information，例如：

- 更高分辨率局部高度图
- 精确接触状态
- 足端与地形的相对几何关系

目的在于提升 PPO 训练稳定性，而 actor 仍保持未来部署可用的输入接口。

---

## 12. 关键实验对比（Ablation）

本方案最值得优先完成的实验如下：

### A1. 是否需要地形信息
- Blind HIM
- HIM + terrain direct concat

### A2. 是否需要独立 terrain branch
- direct concat
- independent terrain branch

### A3. Terrain 是否应进入 internal model
- terrain only in actor
- terrain in actor + internal model

### A4. 是否需要 terrain memory
- no memory
- terrain GRU

### A5. 是否能适应异步 terrain
- 每步更新 terrain
- 每 K 步更新 + hold
- 加噪声 / 丢帧 / age

### A6. 真实感知约束是否需要提前建模
- 理想全可见局部地图
- 加可见域裁剪
- 加 mask + age
- 加外参偏差随机化

---

## 13. 关键评估指标

建议统一记录以下指标：

### 任务表现指标

- 成功通过率
- 平均通过障碍数
- 平均前进距离
- episode return
- 跌倒率

### 行为质量指标

- 速度跟踪误差
- base pitch / roll 稳定性
- foot clearance 分布
- 落脚失败率
- 足端踩入 gap 事件数
- 撞墙事件数

### 学习过程指标

- 收敛速度
- terrain 分支是否被利用（可做消融或特征归因）
- internal model 预测误差（若已实现相关指标）

### 鲁棒性指标

- 在随机视场裁剪下的性能
- 在地图空洞条件下的性能
- 在 terrain 更新延迟下的性能
- 在外参偏差随机化下的性能

---

## 14. 当前最推荐的最小可行版本（MVP）

如果目标是尽快得到一版可验证结果的系统，推荐如下组合：

### MVP-1

- 保留原 HIMLoco proprio/history 主干
- 使用仿真 oracle terrain
- terrain 表示采用 96 点固定高度采样
- 高度采用 robot-centric、gravity-aligned、relative-to-base 表示
- terrain 独立 MLP encoder
- actor 使用后融合
- internal model 暂不接 terrain
- 任务从单个 gap 和低台阶开始
- 从第一版开始就保留 `mask` 与 `terrain_age` 接口

### MVP-2

在 MVP-1 基础上：

- internal model 条件化使用 terrain latent
- 增加连续双 gap / 高台阶任务
- 逐步引入理想视场裁剪

### MVP-3

在 MVP-2 基础上：

- terrain 分支加入 GRU
- terrain 更新改为低频异步
- 加入噪声、空洞、age
- 加入外参偏差随机化

---

## 15. 预期结论与判断标准

若实验顺利，预期应能得到以下判断：

### 结论 1
局部地形几何输入能显著提升连续 gap / 高墙任务表现。

### 结论 2
相比直接拼接观测，独立 terrain branch 更稳定、更易收敛。

### 结论 3
让 terrain 信息进入 internal model，能在更难任务上带来进一步提升。

### 结论 4
连续 gap 对时序记忆更加敏感，terrain GRU 更有价值。

### 结论 5
即使当前不考虑部署，也应在训练后期加入异步更新和 terrain 缺陷建模。

### 结论 6
地形输入更适合采用 robot-centric、gravity-aligned、relative-to-base 的局部表示，而不是全局绝对高度。

### 结论 7
训练早期可以使用理想局部采样区域做方法验证，但中后期必须引入可见域、遮挡、外参误差和信息新鲜度建模。

---

## 16. 当前阶段的结论

综合目前讨论，当前最合理的路线不是：

- 继续坚持纯 blind HIM
- 也不是直接上原始深度图 / 原始点云端到端

而是：

**以 HIMLoco 为高频本体感觉主干，增加独立 terrain branch，先使用仿真 oracle 局部地形几何进行训练，并通过系统化 ablation 验证 terrain 信息在 actor 与 internal model 中的作用。**

同时，在地形表示和采样区域上应遵循：

- 使用 robot-centric、gravity-aligned、relative-to-base 的局部高度表示
- 前期不必严格受真实传感器视场限制
- 但应尽早预留 mask、terrain_age 和外参偏差随机化接口
- 中后期逐步加入真实可见域与遮挡建模

这是当前阶段兼顾：

- 工程可实现性
- 训练稳定性
- 后续部署兼容性
- 研究问题清晰性

的一条最优先路线。

---

## 17. 下一步建议

建议按如下顺序推进：

1. 实现 96 点局部地形采样
2. 将高度定义为相对 base 的局部高度差
3. 跑 blind HIM 与 terrain direct concat 对比
4. 实现独立 terrain branch
5. 实现 terrain-conditioned internal model
6. 加入连续 gap 任务
7. 加入 terrain GRU
8. 加入异步更新、age、mask 与噪声
9. 加入虚拟视场裁剪
10. 加入外参偏差随机化

当以上实验完成后，再回到部署问题，讨论：

- RealSense / Mid-360 的上游映射接口
- 统一 elevation map 表示
- 感知更新频率与控制频率的协同方式
- 真实外参标定与视场覆盖约束
